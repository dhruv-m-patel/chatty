{
  "name": "@alcumus/service-mesh-plugin",
  "version": "0.0.6",
  "main": "build/index.js",
  "scripts": {
    "build": "rimraf ./build && tsc ",
    "test": "jest --runInBand",
    "test:ci": "jest --ci --coverage --coverageReporters=text --coverageReporters=text-summary --passWithNoTests",
    "test:watch": "jest --watch"
  },
  "dependencies": {
    "@alcumus/express-app": "^0.0.6",
    "@alcumus/express-middlewares": "^0.0.6",
    "axios": "^0.21.1"
  },
  "devDependencies": {
    "@types/jest": "^26.0.22",
    "@types/supertest": "^2.0.11",
    "copyfiles": "^2.4.1",
    "jest": "26.6.0",
    "nodemon": "^1.19.1",
    "rimraf": "^3.0.2",
    "supertest": "^6.1.3",
    "ts-jest": "^26.5.4",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  }
}
